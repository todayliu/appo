1、说明
1.1 交互流程：客户端发起请求，js->appAjax->post->(加密数据)->服务器接收->解密->处理－>加密返回。
1.2 加解密：
    需要传的参数存入map字典，按字典key的升序重排，按键值转字符串再拼接来加入唯一的token得到一个sha1的32位码，再加字到字典。接入把字典转为Json字符串再转成base64,用唯一的token和base64做异或运算得到的数据再转base64，此时的64码就是用于传输的字符串。
    解密按着反方向来。


直接在webserver中运行index.php。